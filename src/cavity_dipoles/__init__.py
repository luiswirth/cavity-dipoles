"""Orchestration and shared physics for the EP-GP / BEM cavity benchmark.

All commands are meant to be run from the project root, so data paths are plain
relative paths (out/..., res/...). Run modules with `uv run python -m
cavity_dipoles.<module>` or via the console scripts declared in pyproject.toml.
"""
